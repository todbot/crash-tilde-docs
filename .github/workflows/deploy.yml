name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      deploy:
        description: "Deploy to tilde.crashspace.org"
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install mkdocs mkdocs-terminal

      - name: Build site
        run: mkdocs build

      - name: Deploy via rsync
        if: ${{ inputs.deploy == true }}
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$DEPLOY_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H tilde.crashspace.org >> ~/.ssh/known_hosts
          rsync -avz --delete --exclude='_generated/' site/ ${{ secrets.DEPLOY_USER }}@tilde.crashspace.org:/var/www/tilde.crashspace.org/html/
